# 渐进式脑图 - 手动测试计划

> 覆盖 Phase 1-4 实现的功能：数据迁移、内容选择、流式预览、生成质量优化

---

## 前置准备

```bash
cd /Users/gin/opencode_project/progressive_mindmap
npm run dev
# 打开 http://localhost:5173
```

确保已配置至少一个可用的模型提供商。

---

## 测试 1：数据迁移 (mindmapId → mindmapIds)

**目的：** 验证旧格式数据在刷新后正确迁移

**步骤：**
1. 打开 DevTools → Application → IndexedDB → progressive-mindmap → conversations
2. 如无旧数据，手动添加一条记录，设置 `mindmapId: "test-old-id"`、`mindmapIds: undefined`
3. 刷新页面
4. 检查同一条记录：`mindmapIds` 应为 `["test-old-id"]`，`mindmapId` 应不存在
5. 应用中功能正常（无崩溃）

✅ 通过标准：旧格式自动迁移，不报错

---

## 测试 2：内容选择 - 整条消息勾选

**目的：** 验证勾选框选中/取消选中整条消息

**步骤：**
1. 创建新对话（不关联脑图）
2. 发送一条消息（如 "什么是 React？"）
3. 观察消息左侧的 `□` 按钮
4. 点击 `□` → 应变为 `✓`，消息出现蓝色边框 +「已选为物料」标签
5. 再点 `✓` → 恢复 `□`，蓝色边框和标签消失
6. 勾选 3 条不同的消息 → 所有都显示选中状态
7. 取消其中 1 条 → 另 2 条保持选中

✅ 通过标准：每轮勾选/取消独立，不同消息不互相影响

---

## 测试 3：内容选择 - 右键菜单选中文本片段

**目的：** 验证文本选中后右键添加物料

**步骤：**
1. 选中 AI 回复中的一句话（如 "React 是一个用于构建用户界面的 JavaScript 库"）
2. 右键 → 应弹出菜单「加入脑图物料」
3. 点击「加入脑图物料」→ 物料池中新增该项
4. 检查物料池中该项的文本是选中的那句话（而非整条消息）
5. 未选中任何文本时右键 → 不弹出菜单

✅ 通过标准：右键菜单仅在选中文本时弹出，内容正确

---

## 测试 4：内容选择 - 拖拽到脑图面板

**目的：** 验证拖拽手柄添加物料

**步骤：**
1. 确保脑图面板已打开（顶栏 `PanelRightOpen` 按钮）
2. 鼠标悬停在某条消息上 → 底部出现操作按钮
3. 找到 `⋮⋮` (GripVertical) 拖拽手柄按钮
4. 拖拽手柄到脑图面板区域
5. 观察面板出现蓝色高亮环
6. 释放 → 该消息添加到物料池
7. 拖拽非消息内容到面板 → 无反应

✅ 通过标准：拖拽手柄添加物料，面板有视觉反馈，非物料拖拽被忽略

---

## 测试 5：物料池 UI

**目的：** 验证物料池的展示和操作

**步骤：**
1. 通过方式 2/3/4 添加 3 条物料
2. 打开脑图面板 → 看到 `生成物料 (3)` 可折叠区域
3. 每个物料项显示：来源对话名 → 消息摘要（60 字截断）
4. 点击折叠箭头 → 物料列表折叠/展开
5. 点击某个物料项的 `×` 删除按钮 → 该物料移除，对应消息选中状态清除
6. 点击「清空」→ 所有物料移除，所有消息选中状态清除
7. 关闭脑图面板再打开 → 物料仍然存在（不跨 session 但当前 session 内保持）

✅ 通过标准：物料列表增删正常，折叠/展开独立，清空功能正常

---

## 测试 6：流式预览 - 实时树渲染

**目的：** 验证生成过程中树结构实时更新

**步骤：**
1. 勾选 2-3 条内容丰富的消息作为物料
2. 创建脑图并关联当前对话
3. 打开脑图面板 → 确认物料池有内容
4. 点击「更新图谱」
5. **观察面板树区域**：节点应逐个出现（而非等待后一次性出现）
6. **观察进度条**：面板顶部出现 `已生成 N 个主题 · 深度 X/3`
7. 生成完成后：
   - 进度条消失
   - 树结构保留完整
   - `更新图谱` 按钮恢复可用

✅ 通过标准：生成过程中树实时变化，有进度数字，完成后状态正常

---

## 测试 7：物料优先 vs 降级

**目的：** 验证物料池非空时优先使用物料，空时降级为全对话

**步骤：**
1. 场景 A：物料池有 2 条选中消息 → 点「更新图谱」→ 生成的脑图应仅包含这 2 条消息的知识
2. 场景 B：清空物料池 → 点「更新图谱」→ 应使用全部关联对话生成（不报错）
3. 场景 C：既无物料又无关联对话 → 点「更新图谱」→ 应显示错误提示

✅ 通过标准：三种场景行为符合预期

---

## 测试 8：生成质量 - Few-shot Prompt

**目的：** 验证脑图按概念维度分类而非时间顺序

**步骤：**
1. 选择一段涉及多个技术概念的多轮对话
2. 生成脑图
3. 检查根节点是否概括整体话题
4. 检查二级节点是否按概念分类（如「本地状态」「全局状态」「服务端状态」）
5. 检查三级节点是否为具体技术名称/要点
6. 内容较少的对话 → 生成的树不应强行填满 3 层 10 个子节点（宁少勿滥）

✅ 通过标准：脑图有层次感，概念分类合理，不编造内容

---

## 测试 9：质量校验警告

**目的：** 验证重复节点等问题的检测

**步骤：**
1. 选择一段可能导致 LLM 生成重复内容的对话
2. 生成脑图
3. 检查面板底部是否出现黄色「质量提醒 (N)」区域
4. 警告应列出具体问题（如「发现 2 个重复节点：useState」）
5. 正常的简洁内容生成 → 无警告

✅ 通过标准：有重复时显示警告，正常时不误报

---

## 测试 10：刷新后物料不持久化

**目的：** 验证物料池的设计行为（不跨 session）

**步骤：**
1. 勾选 3 条消息 → 物料池显示 3 项
2. 刷新页面 (Cmd+R)
3. 物料池应为空
4. 消息选中状态全部清除
5. 脑图树结构正常恢复（持久化）
6. 对话消息正常恢复（持久化）

✅ 通过标准：物料清空但核心数据（对话、脑图）不丢失

---

## 自动化测试

```bash
# 单元测试（8 文件，69 用例）
npx vitest run

# 类型检查
npx tsc --noEmit

# LSP 诊断（需先安装 typescript-language-server）
# 预期：29 文件，0 错误
```
